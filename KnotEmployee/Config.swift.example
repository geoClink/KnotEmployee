// Copy this file to Config.swift and fill in your Supabase project credentials.
// Config.swift is gitignored — never commit real keys.
enum Config {
    static let supabaseURL  = "https://your-project-id.supabase.co"
    static let supabaseKey  = "your-anon-public-key"
    static let tenantId     = "your-tenant-uuid"
}
